import 'package:flutter/material.dart';
import '../layout/app_layout.dart';

class DetailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return AppLayout(child: Center(child: Text("게시글 상세 화면 (추후 구현)")));
  }
}
